syntax = "proto3";

package app;

option go_package = "/proto";

message Hello {
    string msg = 1;
}

message Response {
    bool ok = 1;
}

service AppService {
    rpc SayHello (Hello) returns (Response) {}
}